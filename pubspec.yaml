name: alert_x
description: "اپ Alert_X - بدون صفحه ورود"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  fluttertoast: ^8.2.4
  url_launcher: ^6.2.5
  geolocator: ^11.0.0
  web_socket_channel: ^2.4.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.0
  google_maps_flutter: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
